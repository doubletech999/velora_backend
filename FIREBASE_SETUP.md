# Firebase Cloud Messaging Setup Guide

## الخطوات المطلوبة لتشغيل الإشعارات:

### 1. تثبيت Firebase Admin SDK
```bash
composer require kreait/firebase-php
```

### 2. تشغيل Migration لإضافة حقل fcm_token
```bash
php artisan migrate
```

### 3. ملف Firebase Credentials
تم حفظ ملف Firebase credentials في:
- `storage/app/firebase-credentials.json`

**ملاحظة مهمة:** تأكد من أن هذا الملف موجود ولا يتم رفعه إلى Git (يجب إضافته إلى `.gitignore`)

### 4. API Endpoint لتحديث FCM Token

من التطبيق، يجب استدعاء:
```
POST /api/notifications/update-token
Headers: Authorization: Bearer {token}
Body: {
    "fcm_token": "user_fcm_token_here"
}
```

### 5. الإشعارات التلقائية

سيتم إرسال الإشعارات تلقائياً في الحالات التالية:

#### أ) عند إنشاء Route/Camping جديد:
- يتم إرسال إشعار لجميع المستخدمين المسجلين
- النوع: `new_route_camping`
- البيانات: `site_id`, `site_type`, `site_name`

#### ب) عند قبول شخص في رحلة:
- يتم إرسال إشعار للمستخدم المسجل في الرحلة
- النوع: `trip_accepted`
- البيانات: `trip_id`, `trip_name`

### 6. اختبار الإشعارات

يمكنك اختبار الإشعارات من خلال:
1. إنشاء route أو camping جديد من لوحة التحكم
2. قبول حجز رحلة من API أو لوحة التحكم

### 7. Logs

جميع أخطاء الإشعارات يتم تسجيلها في:
- `storage/logs/laravel.log`

---

## ملاحظات:

- إذا فشل إرسال الإشعار، لن يفشل الطلب الأساسي (إنشاء route/camping أو قبول رحلة)
- يجب أن يكون المستخدم قد قام بتحديث `fcm_token` من التطبيق أولاً
- الإشعارات تدعم اللغة الإنجليزية حالياً (يمكن تحسينها لاحقاً)

---

## 📱 ملفات التوثيق للفرونت إند:

للمطورين في الفرونت إند، راجع الملفات التالية:

1. **`FRONTEND_FIREBASE_INTEGRATION.md`** - دليل شامل بالعربية والإنجليزية
   - كيفية الحصول على FCM Token
   - كيفية إرسال Token للـ Backend
   - كيفية التعامل مع الإشعارات الواردة
   - أمثلة كاملة للكود (Flutter/Android/iOS)

2. **`FRONTEND_API_DOCUMENTATION.md`** - توثيق API باللغة الإنجليزية
   - API Endpoint لتحديث FCM Token
   - أنواع الإشعارات المتوقعة
   - Structure البيانات
   - أمثلة على الـ Responses

**أرسل هذه الملفات لمطور الفرونت إند!** 🚀

